<?xml version="1.0"?>
<block>
  <name>source_address_check</name>
  <key>inets_source_address_check</key>
  <category>[inets]</category>
  <import>import inets</import>
  <make>inets.source_address_check($develop_mode, $source_address, $reserved_argument)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>develop_mode</name>
    <key>develop_mode</key>
    <type>int</type>
  </param>

  <param>
    <name>source_address</name>
    <key>source_address</key>
    <type>int</type>
  </param>

  <param>
    <name>reserved_argument</name>
    <key>reserved_argument</key>
    <type>float</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <sink>
    <name>frame_info_in</name>
    <type>message</type>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>frame_info_out</name>
    <type>message</type>
  </source>
</block>
